# Legacy Patch Scripts

These shell scripts were used as one-time fixes during development
(e.g. PDFKit typings, expense module fixes, donate page layout).

They are NOT used by the application at runtime.

Run manually only if needed, and review before executing
because some scripts contain hardcoded paths like /var/www/Bekatam.
